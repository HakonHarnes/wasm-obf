version: "3"
services:
  mongodb:
    image: mongo:latest
    container_name: mongodb
    ports:
      - "27017:27017"
    networks:
      - db_network
    volumes:
      - mongodb_data:/data/db
networks:
  db_network:
    external: true
volumes:
  mongodb_data:
